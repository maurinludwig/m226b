package com.mlns.terminal;

/**
 * defines class bus
 */
public class Bus extends Vehicle {

    public Bus(int capacity, boolean firstClass, Platform platform, String to) {
        super(capacity, firstClass, platform, to);
    }
}
